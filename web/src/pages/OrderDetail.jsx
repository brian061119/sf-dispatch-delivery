// Owner: Y (order detail + confirm receipt + review).
// Placeholder: route works, implementation pending. Use api/order.js (getOrder / confirmReceipt / submitReview).
export default function OrderDetail() {
    return (<div style={{ padding: 48 }}>
      <h2>Order Detail</h2>
      <p>Placeholder — owner: Y. Implementation pending.</p>
    </div>);
}
